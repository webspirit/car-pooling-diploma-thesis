<h1>Listing points</h1>

<table>
  <tr>
    <th>Departure lat</th>
    <th>Departure lng</th>
    <th>Arrival lat</th>
    <th>Arrival lng</th>
    <th>Departure address</th>
    <th>Arrival address</th>
    <th>Distance departure range</th>
    <th>Distance arrival range</th>
    <th>Start time</th>
    <th>Time range from</th>
    <th>Time range to</th>
    <th>Active</th>
    <th>User</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @points.each do |point| %>
  <tr>
    <td><%= point.departure_lat %></td>
    <td><%= point.departure_lng %></td>
    <td><%= point.arrival_lat %></td>
    <td><%= point.arrival_lng %></td>
    <td><%= point.departure_address %></td>
    <td><%= point.arrival_address %></td>
    <td><%= point.distance_departure_range %></td>
    <td><%= point.distance_arrival_range %></td>
    <td><%= point.start_time %></td>
    <td><%= point.time_range_from %></td>
    <td><%= point.time_range_to %></td>
    <td><%= point.active %></td>
    <td><%= point.user_id %></td>
    <td><%= link_to 'Show', point %></td>
    <td><%= link_to 'Edit', edit_point_path(point) %></td>
    <td><%= link_to 'Destroy', point, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Point', new_point_path %>
