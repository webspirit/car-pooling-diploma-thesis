Car pooling is an application for mobile devices that finds matching routes added by users, so that users (driver-passenger) can go together to their destination.
The application's back-end: Ruby on Rails / front-end: jQuery mobile 
